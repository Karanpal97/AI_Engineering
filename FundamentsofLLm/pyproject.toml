[project]
name = "fundamentsofllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Karanpal97", email = "karangill1810@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=7.3.0",
    "openai>=2.53.0",
    "python-dotenv>=1.2.2",
    "yfinance>=1.6.0",
]

[project.scripts]
fundamentsofllm = "fundamentsofllm:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
